#include <iostream>
#include <cstdlib>

using std::cin;
using std::cout;
using std::endl;

auto
main(void) -> int {
    long long int L {}, R {};
    cin>>L>>R;
    int N {};
    cin>>N;
    for (int i {}; i < N; i += 1) {
        long long int l {}, r {};
        cin>>l>>r;
    }
    return EXIT_SUCCESS;
}