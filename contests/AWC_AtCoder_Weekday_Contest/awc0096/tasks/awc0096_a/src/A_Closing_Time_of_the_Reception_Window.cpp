#include <iostream>
#include <cstdlib>

using std::cin;
using std::cout;
using std::endl;

auto
a_closing_time_of_the_reception_window(
    const int& N
) -> void {
    for (int i {}; i < N; i += 1) {
        long long int A {}, B {};
        cin>>A>>B;
    }
}

auto
main(void) -> int {
    int N {};
    cin>>N;
    return EXIT_SUCCESS;
}