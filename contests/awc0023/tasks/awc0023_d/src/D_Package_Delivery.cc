#include <iostream>
#include <cstdlib>

using std::cin;
using std::cout;
using std::endl;

auto
main(void) -> int {
    int N {}, S {}, T {};
    cin>>N>>S>>T;
    for (int n {}; n < N; n += 1) {
        int P {}, C {}, W {};
        cin>>P>>C>>W;
    }
    return EXIT_SUCCESS;
}